[project]
name = "llm-xai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.8.4",
    "torch>=2.11.0",
    "transformer-lens>=2.16.1",
    "transformers>=5.5.0",
]
